import 'package:flutter/material.dart';

class AppDecorations {
  static const BoxDecoration greyRoundedCard = BoxDecoration(
    color: Colors.grey,
    borderRadius: BorderRadius.all(Radius.circular(8)),
  );
}
